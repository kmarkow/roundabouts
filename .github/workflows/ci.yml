name: Manual trigger

on:
  workflow_dispatch:
    inputs:
      name:
        description: "Who to greet"
        default: "World"

jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
          - name: Hello Step
            env:
              CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
            run: echo "testing:" && echo $CLIENT_SECRET
